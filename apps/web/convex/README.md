Convex is initialized for `apps/web`.

- Run `pnpm convex:dev` in `apps/web` during development.
- Generated types live in `convex/_generated/`.
- Start adding schema, queries, mutations, and actions directly in this folder.
